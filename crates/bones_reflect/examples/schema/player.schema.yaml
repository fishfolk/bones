struct:
  name:
    primitive: string
  height:
    primitive: f32

  atlas:
    struct:
      tile_size:
        struct:
          x:
            primitive: u32
          y:
            primitive: u32
      columns:
        primitive: u32
      rows:
        primitive: u32

  favorite_things:
    vec:
      primitive: string
